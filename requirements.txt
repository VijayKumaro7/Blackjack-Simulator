# Runtime dependencies (no external packages required for core simulation)

# Optional — for the Jupyter analysis notebook
matplotlib>=3.7
numpy>=1.24
jupyter>=1.0
notebook>=7.0

# Development / testing
pytest>=7.4
pytest-cov>=4.1
